<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thư mục: <%= folder.name %></title>
</head>
<body>
    <h1>Thư mục: <%= folder.name %></h1>
    
    <h2>Tệp trong thư mục này:</h2>
    <ul>
        <% folder.files.forEach(file => { %>
            <li>
                <%= file.name %> 
                (<%= (file.size / 1024).toFixed(2) %> KB)
                <a href="/files/download/<%= file.id %>">Tải xuống</a>

                <form action="/files/files/<%= file.id %>?_method=DELETE" method="POST" style="display: inline;">
                  <button type="submit">Xóa</button>
                </form>
            </li>
        <% }) %>
    </ul>

    <h2>Tải lên tệp mới:</h2>
    <form action="/files/folders/<%= folder.id %>/upload" method="POST" enctype="multipart/form-data">
        <input type="file" name="file" required>
        <button type="submit">Tải lên</button>
    </form>

    <a href="/files/folders">Quay lại danh sách thư mục</a>
</body>
</html>